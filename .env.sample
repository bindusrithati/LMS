# SMTP Configuration (Example values - replace with your own)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-or-gmail-password
SMTP_FROM_EMAIL=your-email@gmail.com

# PostgreSQL Database Configuration (Example values)
POSTGRES_DB=your_database_name
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATA_PATH=./pg_data

# Redis Configuration (Example values)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=your-64-character-secret-key-here-make-sure-its-random-and-secure

# Frontend URLs (Example values)
FRONTEND_RESET_URL=http://127.0.0.1:8000/reset-password
